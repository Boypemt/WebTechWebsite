# Claude Design exports
# Drop exported HTML files from claude.ai/design here.
# Ignored from git — design files are workflow references, not source code.
design/

# Node
node_modules/

# SQLite database — runtime data, not source code
store.db
store.db-shm
store.db-wal

# Environment variables — never commit secrets
.env

# OS files
.DS_Store
Thumbs.db

# Editor directories
.vscode/
